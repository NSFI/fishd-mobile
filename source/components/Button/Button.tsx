import * as React from 'react';
import { findDOMNode } from 'react-dom';
import * as PropTypes from 'prop-types';

class Button extends React.Component {
  render() {
    return (
      <button>demo按钮</button>
    );
  }
}

export default Button;
